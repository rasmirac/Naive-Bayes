__author__ = 'RachelSmith'
import random
myList = ['cat', 'kitty', 'dog', 'blerrg']

myDict = {'cat': 'meow'}
newListOne = random.sample(myList, len(myList))

newListTwo = random.sample(myList, len(myList))

print len(myList[0])